%h1&= "Table #{params[:table]}"
%a{:href=>url('')} goto queries
%a{:href=>table_scheme_url(params[:table])} goto schema
%form(method="get")
  = connection_field
  = per_page_field
  %br
  %label(for='where') Where:
  %br
  %textarea.where(name="where" cols="60" rows=9)&= params[:where]
  %br
  %input(type="submit")
= haml :rezult, :layout => false
